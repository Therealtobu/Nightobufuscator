cd \~/Nightotobufuscator
cat > requirements.txt << EOF
discord.py>=2.4.0
python-dotenv
Flask
requests
EOF
git add requirements.txt && git commit -m "Add requests for 0x0.st upload" && git push origin main
